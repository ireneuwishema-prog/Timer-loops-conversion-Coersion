# Timer-loops-conversion-Coercion

# Timer about to setTimeout ans their interval the time it will take 

# loop help us to don't write many codes or reaping your self

# conversion about to convert one type to another and it done by us international

# coercion about to convert  one type behind the scene and it happen automatically  by language

# filter we use it when we need to remove the number withe negative sine

# foreach it used when you want to perform action for each item without creating a new array  (it means it execute all function one time not calling every function )

# map  transform those element and return new array

# sort this will arrange the numbers to order from smallest to highest - from biggest to smallest + from smallest to  higher 

# slice remove one to one  from end point to end point
# splice it remove from start point to end point  and it can be used to replce the number you deleted 
# includes check if the thing is exist it will give us true if it is not false
#
# split will change the string onto an array